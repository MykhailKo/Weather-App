<!DOCTYPE html>
<html>
    <head>
        {{>css_links}}
        <title>Weather nodes</title>
    </head>
    <body>
        {{>header}}
        <section class="page-content">
            <h1>Help</h1>
            <p>{{message}}</p>
        </section>
        {{>footer}}
    </body>
</html>